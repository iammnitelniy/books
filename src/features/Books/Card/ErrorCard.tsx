import {CardStyle} from "../../../common/styles/StyledComponents.tsx";

export const ErrorCard = () => {
    return (
        <CardStyle>
            Book is not loaded
        </CardStyle >
    );
};

